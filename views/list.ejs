<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width initial-scale=1 shrink-to-fit-no">
        <title></title>
        
        <link rel="stylesheet" href="css/styles.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
        
    </head>

    <body>
        <div class="d-flex flex-row justify-content-start">
            <div class="lists-wrapper">
                <div class="box lists">
                    <h1>Your lists</h1>
                    <ul>
                        <% for (var i=0;i<allLists.length;i++){ %>
                            <li><%=allLists[i].name%></li>
                        <%}%>
                    </ul>
                </div>
            </div>
            <div class="main-content">
                <div class="box" id="heading">
                    <h1><%= day %></h1>
                </div>
                <div id="tasks" class="box">
                    <%if (todos!=null){%>
                        <% for (var i=0;i<todos.length;i++){ %>
                            <form method='POST' action='/<%=collectionName%>/delete'> 
                                <div class="item">
                                    <input type="checkbox" name="itemId" value="<%=todos[i]._id%>" onChange="this.form.submit()">
                                    <p><%=todos[i].name%></p>
                                </div>
                            </form>                   
                            <%}%>
                    <%}else{%>
                        <div class="item"><p>Empty list</p></div>
                    <%}%>
        
                    <form  class="item" action="/<%=collectionName%>" method="POST">
                        <input type="text" name="newItem" id="newItem" placeholder="Add item" autocomplete="off">
                        <button type="submit" name="button">+</button>
                    </form>
                </div>
            </div>
        </div>            
        
    
           
           
           <!-- <li>Work on personal project</li>
           <li>Clean bedroom</li> -->
       </ul>
       <% if (message!=''){message}%>

       

    </body>
</html>